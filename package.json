{
  "name": "as-downtime",
  "private": true,
  "version": "3.10.5",
  "type": "module",
  "scripts": {
    "build": "node scripts/fetch-public.mjs",
    "dev": "npx vercel dev",
    "deploy": "npx vercel deploy --prod"
  },
  "engines": { "node": ">=20" }
}
